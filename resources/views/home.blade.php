<x-guest-layout>

    <!-- HERO -->
    @include('home.hero')

    <!-- SERVICES -->
    @include('home.services')

    <!-- COLLECTIONS -->
    @include('home.collections')

    <!-- ABOUT -->
    @include('home.about')

</x-guest-layout>